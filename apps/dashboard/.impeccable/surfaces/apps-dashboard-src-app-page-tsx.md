---
version: 1
slug: "apps-dashboard-src-app-page-tsx"
primary_target: "apps/dashboard/src/app/page.tsx"
related_targets: ["apps/dashboard/src/app/demo.css","apps/dashboard/src/components/demo/PublicDemo.tsx","apps/dashboard/src/components/agent/AgentParticleScene.tsx"]
---

# Mandate public guided demonstration

## Scope and mode

- Target: `apps/dashboard/src/app/page.tsx`
- Mode: Persuade
- Audience: a first-time hackathon evaluator, agent builder, or principal with no protocol background
- Job: understand in under three minutes why outcome-bound authority differs from plans and permissions
- Primary action: run the six-stage governed checkout simulation, then inspect the proof-backed live record or source
- Proof: the completed checkout Mandate, exact action/checksum/evidence digests, merged PR, deployed AWS control plane, and explicit remaining limits
- Constraint: the public simulation executes nothing and must remain visibly distinct from the authenticated `/dashboard` record

## Direction contract

**THESIS:** Show the authority mechanism operating, not a dashboard describing that it operated. A guided action token moves from delegation through interception, denial, replanning, separate human approval, independent verification, and immutable completion while the authority envelope never changes.

**OWN-WORLD:** Extend the pinned near-black mission-control instrument: matte graphite fields, steel hairlines, compact Inter, cyan in-scope energy, amber human review, blocked red only at a stopped expansion, and evidence green only after independent verification. Broad regions replace equal card grids.

**STORY:** The visitor first learns the distinction between outcome, plan, and authority; runs a six-stage simulation; sees one forbidden database effect stopped and a compliant plan substituted; traces the same action through the real architecture; then inspects exact proof and honest limitations.

**FIRST VIEWPORT:** A compact public header frames a split instrument. The left states “Delegate outcomes, not tool calls” and defines the promise in plain language. The right is a live authority map: an immutable envelope surrounds a changing plan, a decision gate, and the procedural portrait. One prominent button begins the guided run.

**FORM:** Assigned architecture map with moving action token, fused with the user-required three-minute guided narration; first of the dealt structures. Seed key: `abf7bb87`.

**FINISH:** unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance

## Memorable moment

A proposed database mutation strikes the fixed authority boundary, turns the portrait field red, and stops. The plan rewrites itself to a repository-only repair while the authority envelope visibly remains unchanged.

## Unresolved decisions

Alexa+ and AgentCore remain roadmap integrations and must appear as unconnected reference boundaries, never as live claims. The authenticated operator record remains separately protected at `/dashboard`.
